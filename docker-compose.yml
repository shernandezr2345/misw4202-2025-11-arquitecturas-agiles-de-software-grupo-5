version: "3.8"

services:
  flask_app:
    build: .
    container_name: flask_api
    restart: always
    environment:
      FLASK_ENV: development
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    command: ["python", "app.py"]